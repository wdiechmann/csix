<%= simple_form_for(@jobber) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :first_job_at %>
    <%= f.input :last_job_at %>
    <%= f.association :person %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
