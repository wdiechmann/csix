<%= form_for @jobber, :html => { :class => 'form-horizontal form-model' } do |f| %>
  <div class="form-group">
    <%= f.label :first_job_at, :class => 'control-label col-md-2' %>
    <div class="col-md-10">
      <%= f.datetime_select :first_job_at, :class => 'text-field form-control' %>
    </div>
  </div>
  <div class="form-group">
    <%= f.label :last_job_at, :class => 'control-label col-md-2' %>
    <div class="col-md-10">
      <%= f.datetime_select :last_job_at, :class => 'text-field form-control' %>
    </div>
  </div>

  <div class="form-group">
    <div class='col-md-offset-2 col-md-10'>
      <%= f.submit nil, :class => 'btn btn-primary' %>
      <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                  jobbers_path, :class => 'btn btn-default' %>
    </div>
  </div>
<% end %>
